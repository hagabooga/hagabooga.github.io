extends TouchScreenButton

#
#func _input(event):
#	if event is InputEventScreenDrag or (event is InputEventScreenTouch and event.is_pressed()):
#		if Rect2(global_position, normal.get_size()).has_point(event.position):
#			print("PRESSING", name) 
